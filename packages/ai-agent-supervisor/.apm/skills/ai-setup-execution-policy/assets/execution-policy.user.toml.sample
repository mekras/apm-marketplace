schema_version = 1

# Этот файл хранится в приватном пользовательском слое, а не в Git проекта.
required_evidence = ["результат-проверки"]

[[routes]]
id = "эталонный-маршрут"
environment = "активная-среда"
model = "<приватный-идентификатор-модели>"

[[routes]]
id = "кандидатный-маршрут"
environment = "активная-среда"
model = "<приватный-идентификатор-модели>"

# Файлы свидетельств квалификации можно хранить рядом с этим ядром.
qualification_files = []
