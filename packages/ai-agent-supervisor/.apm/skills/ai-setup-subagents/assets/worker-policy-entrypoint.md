<!-- ai-setup-subagents:runtime-policy:start -->
## Рабочая политика подагентов

До чтения рабочих входов, исследования или реализации сначала проверьте,
существует ли `subagents.local.toml`, и прочитайте его. Политика в этом файле
— единственный источник проектных условий передачи, прямого выполнения,
ограничений входа, приёмки и эскалации.

Разделяйте родительскую задачу и каждое действие или подзадачу внутри неё.
Сначала определите, что родитель должен получить в целом, затем сопоставляйте
каждую потенциально делегируемую подзадачу с отдельным маршрутом. Записи
`direct_execution` относятся к родительской задаче целиком и не являются
безусловным запретом на ограниченный read-only анализ внутри неё. Поэтому
родительская задача, меняющая защищённый источник, может передать отдельный
ограниченный read-only анализ, сохранив запись, утверждение решения и саму
правку у родителя.

До самостоятельного исследования или реализации сопоставьте каждую подзадачу с
маршрутами из `[policy_runtime.routes]`. Если маршрут совпал, выделите
ограниченную подзадачу и запустите его указанный класс через команду из `launcher` с
`--target`, равным `target`, передав все разрешённые рабочие файлы явными
`--input`. Не заменяйте этот запуск встроенным ожиданием или неименованным
вызовом подагента. Дождитесь терминального кода запускателя и прочитайте
созданную запись запуска до использования результата или перехода к прямой
работе. После содержательной проверки внесите в запись отдельные поля
`acceptance` и `quality_assessment` с критериями и материалами проверки.
Если вся родительская задача совпала с `direct_execution`, выполните её у
родителя целиком. Для отдельной подзадачи это условие не переносится: при
подходящем маршруте передача допустима, а прямое выполнение требует отсутствия
подходящего маршрута либо отдельного обоснования, что постановка, вход и
приёмка дороже ожидаемой пользы. В последнем случае сохраните причину в
итоговом следе. Используйте результат класса только после родительской приёмки
и сохраните технический след. Отделяйте отказ дочернего процесса от успешного
процесса без подтверждения модели: для первого применяйте
`execution_failure`, для второго — `unconfirmed_model`. Не повторяйте тот же
запуск без изменения маршрута или среды. При недоступном маршруте действуйте
по `unavailable_route`; не объявляйте настройку, модель, качество или экономию
подтверждёнными без требуемого в политике свидетельства.

Перед выбором маршрута сопоставьте его с фактическими параметрами текущего
родителя из метаданных активной сессии. Поля `policy_design` и сохранённые
назначения описывают историческое основание настройки, а не текущую модель или
усилие. Если параметры неизвестны, сравнение остаётся нерешённым и это нельзя
выдавать за подтверждение. Если модель или усилие родителя изменились,
предыдущее сравнение недействительно и маршрут нужно оценить заново.
Совпадение моделей само по себе не оправдывает передачу и не запрещает её:
проверьте пользу отделённого контекста, полномочия, требуемую независимость,
приёмку и полную стоимость именно для выделенной подзадачи.
<!-- ai-setup-subagents:runtime-policy:end -->
